# Runtime
Django~=6.1.1
psycopg[binary]~=3.2          # PostgreSQL driver -- docs/adr/0004-database.md
python-dotenv~=1.1            # loads .env into the environment
dj-database-url~=3.0          # DATABASE_URL -> Django DATABASES

# Test stack -- docs/adr/0005-testing.md budgets 3-5 packages; this is 5.
# Adding a sixth means revisiting that ADR. scripts/adr_guards.py enforces the
# count, and CI runs it on every push.
# Faker arrives transitively via factory_boy and is not counted.
pytest~=9.1
pytest-django~=4.14
pytest-cov~=7.0
factory_boy~=3.3
pytest-playwright~=0.7        # unblocked by ADR 0006; drives tests/e2e/

# Django REST Framework is deliberately absent -- docs/adr/0003-backend.md
# defers it. scripts/adr_guards.py fails if it appears here.
